{
    "database": {
        "path": "data/app.sqlite",
        "migrationsDir": "server/db/migrations",
        "migrate": "bun run db:migrate",
        "validate": "bun run db:health"
    },
    "install": "bun install",
    "healthCheck": "curl -f http://localhost:3737/api/health"
}
